import { renderSectionPage } from '../../components/section-page.js';

/** @param {import('../../types.ts').RenderContext} context */
export default function renderCultura(context) {
  return renderSectionPage(context, { sectionId: 'cultura' });
}
